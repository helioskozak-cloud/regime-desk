name: Keep Render warm

on:
  schedule:
    - cron: "*/10 13-20 * * 1-5"  # every 10 min, 1:30–8 PM ET, weekdays

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping cloud API
        run: curl -sf https://regime-desk.onrender.com/api/ping || true
